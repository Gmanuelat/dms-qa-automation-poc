{
  "name": "dms-qa-automation-poc",
  "version": "1.0.0",
  "private": true,
  "description": "QA Automation POC: Playwright UI + API + CI for a DMS-style web app",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report",
    "lint:types": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.6.2"
  }
}
